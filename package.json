{
  "name": "invoic-bill-desk",
  "version": "1.0.0",
  "description": "Fullstack Billing Application",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node setup_db.js && node server.js"
  }
}
